{
  "name": "kite",
  "description": "KITE Local — KML Interactive Tree Explorer. Se edita en src/ y se distribuye como un único kitelocal.html.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check",
    "watch": "node build.js --watch",
    "test": "node tests/run-all.js",
    "icons": "node fetch-icons.js",
    "test:browser": "node tests/browser/app.mjs && node tests/browser/clipboard.mjs && node tests/browser/geojson-html.mjs",
    "browser": "playwright install chromium"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "html-minifier-terser": "7.2.0",
    "linkedom": "^0.18.12",
    "playwright": "1.61.1"
  }
}
